import sum from "./second.js";
sum(3,4);
console.log("hello i am first");
// by default Nodejs supports CJS (require wala), import export wala latest module which is called MJS which is not default behaviour of node , simply give mjs extension to the file 