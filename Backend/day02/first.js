const {sum,sub,mul} = require("./calculate");// nodeJs me by default 'index.js' (if present inside the Calculate file)ko export karwayega agar sirf file mentioned hai toh 
sum(2,4);
sub(3,4);
mul(2,1);

console.log("hello");