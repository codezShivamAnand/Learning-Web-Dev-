// npm init 
// npm install express

// to automatically detection of changes by server, we install nodeman 
// npm install -g nodemon // globally installed in the system  
// or 
// sudo npm install -g nodemon